
int l,r,sum,maxlength=0;
 while(r<n)
{
sum=sum+arr[r];
while(sum>k)
{
sum=sum+a[l];
l--;

}
if(sum<=k)
{
maxlength=Math.max(maxlength,i-j+1);
}
r++;

}

##optimized approach
 while(r<n)
{
sum=sum+arr[r];
if(sum>k)
{
sum=sum+a[l];
l--;

}
if(sum<=k)
{
maxlength=Math.max(maxlength,i-j+1);
}
r++;

}
